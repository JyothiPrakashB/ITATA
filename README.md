# Instruct-ATE-ATSC-ASPE

Create environment
```
coda create -n envname python=3.9 -y
```
Install requirements
```
pip install -r requirements.txt
```
Run the notebooks and change the dataset path accoerding to directory structure 
